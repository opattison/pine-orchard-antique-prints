<!-- /snippets/collection-grid-item.liquid -->

{% assign isEmpty = false %}
{% assign collection_item_width = 'large--one-half' %}
<div class="grid__item {{ collection_item_width }} text-center">
  {% comment %}
    Use capture to put the collection title in a variable that is used in liquid filters
  {% endcomment %}
  {% capture collection_title %}{{ collection.title | escape }}{% endcapture %}

  <a href="{{ collection.url }}" class="grid__link-block">
    <div class="grid__image" style="background-image: url('{% if collection.image %}{{ collection.image.src | img_url: 'large' }}{% else %}{{ collection.products.first.featured_image | img_url: 'large' }}{% endif %}')">
    </div>
    <p class="h5">
      <span class="small-cap">{{ collection.title }}</span> – {{ 'collections.general.items_with_count' | t: count: collection.all_products_count }}
    </p>
  </a>

</div>
